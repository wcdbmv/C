#ifndef __IO_H__
#define __IO_H__

#include <stdio.h>
#include <stdlib.h>
#include <stddef.h>

int read_arrays(int **a, size_t *n, int **b, size_t *m);
void write_array(int *a, size_t n);

#endif // __IO_H__
